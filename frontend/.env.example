# No se requieren variables por ahora: en desarrollo Vite hace proxy de /api a http://localhost:8080
# (ver server.proxy en vite.config.ts) y en producción el backend sirve el frontend desde el mismo origen.
